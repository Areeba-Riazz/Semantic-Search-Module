# Data Directory

This directory stores **uploaded text documents** that users provide through the GUI.

- Files are saved here automatically when uploaded via the Streamlit interface.
- The directory is **never pre-populated** — all data comes from user uploads.
- Supported format: `.txt` plain-text files.

> ⚠️ Do NOT hard-code any datasets into this directory.
